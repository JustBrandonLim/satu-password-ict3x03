pipeline {
    agent any

    stages {
        stage('Clean Up') {
            steps {
                echo "Cleaning up..."
                sh 'docker compose down webapp'
                // sh 'docker stop webapp'
                // sh 'docker rm webapp'
                // sh 'docker rmi webapp:latest'
            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'docker compose build webapp'
                // sh 'docker build -t webapp:latest ./webapp/'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                sh 'docker compose up webapp -d'
                // sh 'docker run -d --name webapp --network satu-password-network -p 3000:3000 webapp:latest'
            }
        }
    }
}
